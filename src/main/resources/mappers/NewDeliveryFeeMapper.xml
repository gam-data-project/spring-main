<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.repository.NewDeliveryFeeMapper">

    <insert id="insertNewDeliveryFee" parameterType="org.example.domain.NewDeliveryFee"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO new_delivery_fee
        (
        order_number,
        platform,
        shipping_included,
        total_delivery_fee,
        shipping_count,
        order_date
        )
        VALUES
        (
        #{orderNumber},
        #{platform},
        #{shippingIncluded},
        #{totalDeliveryFee},
        #{shippingCount},
        #{orderDate}
        )
    </insert>

</mapper>
