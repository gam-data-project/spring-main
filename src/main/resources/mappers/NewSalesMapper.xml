<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.repository.NewSalesMapper">

    <insert id="insertNewSales" parameterType="org.example.domain.NewSales"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO new_sales_data
        (
        order_number,
        platform,
        product_name_raw,
        product_id,
        quantity,
        product_total,
        unit_price,
        shipping_included,
        order_date
        )
        VALUES
        (
        #{orderNumber},
        #{platform},
        #{productNameRaw},
        #{productId,jdbcType=BIGINT},
        #{quantity},
        #{productTotal},
        #{unitPrice},
        #{shippingIncluded},
        #{orderDate}   <!-- 문자열 'YYYY-MM-DD' 또는 LocalDate 사용 -->
        )
    </insert>

</mapper>
